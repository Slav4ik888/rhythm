import { FC, memo } from 'react';
import { RootAuthComponent } from './compponent';



const RootAuthContainer: FC = memo(() => {
  
  
  return (
    <RootAuthComponent />
  );
});

export default RootAuthContainer;
